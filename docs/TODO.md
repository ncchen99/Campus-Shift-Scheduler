# 代辦清單

- [ ] 在手機頁面上設定頁面跑版問題，取消管理權限會跑到卡片外，新增管理員的2個輸入框高度不夠
- [ ] 自動勾選當日時段的功能應該改成按第一個 radio button 會全部設定，然後再次點選當日任一個(包含第一個) radio button 那一個時段都會取消勾選(其他保持不變)
- [ ] 並且在這個填寫沒空時間的頁面上也新增一個可以控制是否開啟這個同日自動勾選

- [] [?] AI 排班表
- [] 填寫沒空時間的頁面設計成每5秒檢查畫面上的元素是否有更改有的話自動儲存到資料庫，包含特殊需求說明